# 医院局域网服务器部署指南 (Hospital Intranet Deployment Manual)

本指南专为**医院信息科/IT部门管理员**编写，旨在指导如何将“动态心电图 PDF 转 Excel 智能网页系统”部署到医院局域网服务器（支持 Linux Server、Windows Server 或 Docker），实现科室全体医生电脑/iPad 免安装、通过内网网址统一访问使用。

---

## 方案 1：Docker 一键容器化部署 (推荐，最省心)

适合 Linux (Ubuntu/CentOS/RedHat) 或 Windows Server (已装 Docker Desktop)。支持开机自启、隔离运行、零依赖冲突。

### 部署步骤：
1. 将整个 `pdf转excel` 文件夹拷贝到服务器任意目录（例如 `/opt/ecg-app`）；
2. 在该目录下打开终端并运行：
   ```bash
   docker-compose up -d --build
   ```
3. 查看容器状态：
   ```bash
   docker ps
   ```
   看到 `ecg_pdf_parser_web` 容器状态为 `Up` 即表示部署成功！

---

## 方案 2：Windows Server 局域网部署 (开机自启服务)

适合医院使用 Windows Server 2016 / 2019 / 2022 作为服务器的情况。

### 部署步骤：
1. **安装 Python 3.12**：从官方下载安装，勾选 `Add Python 3.12 to PATH`；
2. **复制目录**：将 `pdf转excel` 文件夹复制到 Windows Server 的 `C:\ecg-app`；
3. **配置 Waitress 生产服务器**：
   在终端（Command Prompt）中执行：
   ```cmd
   cd C:\ecg-app
   python -m venv venv
   call venv\Scripts\activate.bat
   pip install -r requirements.txt
   ```
4. **启动生产级 Waitress 服务**：
   ```cmd
   waitress-serve --host=0.0.0.0 --port=5000 app:app
   ```
5. *(可选)* **配置为 Windows 开机自启动服务**：
   使用免费工具 [NSSM (Non-Sucking Service Manager)](https://nssm.cc/)：
   ```cmd
   nssm install ECGParserService "C:\ecg-app\venv\Scripts\python.exe" "C:\ecg-app\app.py"
   nssm start ECGParserService
   ```

---

## 方案 3：Linux (Ubuntu/CentOS) 原生部署 (Gunicorn + Systemd)

适合 Linux 物理服务器或虚拟机。

### 1. 初始化 Python 环境
```bash
cd /opt/pdf转excel
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### 2. 配置 Systemd 开机自启服务
创建服务配置文件：
```bash
sudo nano /etc/systemd/system/ecg-app.service
```
粘贴以下内容：
```ini
[Unit]
Description=ECG PDF Parser Web App
After=network.target

[Service]
User=root
WorkingDirectory=/opt/pdf转excel
ExecStart=/opt/pdf转excel/venv/bin/gunicorn --bind 0.0.0.0:5000 --workers 4 --threads 4 --timeout 600 app:app
Restart=always

[Install]
WantedBy=multi-user.target
```
启动并开启开机自启：
```bash
sudo systemctl daemon-reload
sudo systemctl start ecg-app
sudo systemctl enable ecg-app
```

---

## 3. 防火墙配置与全网段访问验证

无论采用哪种部署方案，都需确保服务器的 **5000 端口** 在局域网防火墙中开放：

* **Windows 防火墙**：在“高级安全 Windows 防火墙”中新建入站规则 $\rightarrow$ 开放 TCP 端口 `5000`。
* **Linux (ufw/firewalld)**：
  ```bash
  # ufw 例子:
  sudo ufw allow 5000/tcp
  # firewalld 例子:
  sudo firewall-cmd --zone=public --add-port=5000/tcp --permanent
  sudo firewall-cmd --reload
  ```

---

## 4. 医生端使用验证 (无需任何配置)

部署完成后，IT 管理员只需将**服务器的内网 IP 地址**发送给医生（例如 `http://192.168.1.100:5000`）：

* 医生在办公室电脑的 Chrome / Edge 浏览器打开该网址；
* 拖入包含 PDF 报告的 ZIP 压缩包，点击“开始多进程高并发解析”；
* 解析完成后直接下载 `.xlsx` Excel 汇总表！
