# 动态心电图 PDF 转 Excel 智能网页系统 · 全平台全模式部署指南大全

本指南涵盖了系统在**Mac 单机**、**Windows 单机**、**医院局域网服务器**、**云服务器公网部署**以及**域名反向代理/HTTPS**的所有部署方案。您可以根据具体的实际使用场景选择对应的部署章节。

---

## 目录
1. [场景一：Mac 电脑单机一键双击使用](#1-场景一mac-电脑单机一键双击使用)
2. [场景二：Windows 电脑单机一键双击使用](#2-场景二windows-电脑单机一键双击使用)
3. [场景三：公网云服务器 Docker 容器化部署 (推荐公网)](#3-场景三公网云服务器-docker-容器化部署-推荐公网)
4. [场景四：Linux 云服务器 (Ubuntu/CentOS) 原生部署 + 后台守护进程](#4-场景四linux-云服务器-ubuntucentos-原生部署--后台守护进程)
5. [场景五：Windows Server 服务器生产级部署 (NSSM 服务)](#5-场景五windows-server-服务器生产级部署-nssm-服务)
6. [场景六：医院局域网多台电脑共享访问](#6-场景六医院局域网多台电脑共享访问)
7. [进阶补充：阿里云/腾讯云安全组放行 & Nginx 绑定域名/HTTPS](#7-进阶补充阿里云腾讯云安全组放行--nginx-绑定域名https)

---

## 1. 场景一：Mac 电脑单机一键双击使用

适合医生在自己的 Mac 苹果电脑上单机独立使用，无需连接外网服务器。

### 操作步骤：
1. 将桌面上的 `pdf转excel` 文件夹存放在 Mac 上；
2. 双击运行文件夹中的 **`启动心电转换系统.command`** 图标；
3. 系统会自动配置 Python 虚拟环境，并**自动调起 Safari/Chrome 浏览器**打开 `http://127.0.0.1:5000`；
4. 拖入 PDF 或 ZIP 压缩包解析下载；用完后直接关闭浏览器和终端窗口即可。

---

## 2. 场景二：Windows 电脑单机一键双击使用

适合医生在自己的 Windows 10/11 电脑上单机独立使用。

### 前置条件：
* 确保 Windows 电脑上安装了 Python（如未安装，前往 [python.org](https://www.python.org/downloads/) 下载，安装时务必勾选 `Add Python to PATH`）。

### 操作步骤：
1. 双击运行文件夹中的 **`启动心电转换系统.bat`** 脚本图标；
2. 系统会自动初始化环境，并**自动调起 Edge/Chrome 浏览器**打开 `http://127.0.0.1:5000`；
3. 拖入文件解析并导出 Excel，使用完毕后关闭命令行窗口即可。

---

## 3. 场景三：公网云服务器 Docker 容器化部署 (推荐公网)

适合部署在阿里云、腾讯云、华为云等云服务器（Linux/Windows均可），实现**开机自启、隔离运行、高并发防护**。

### 操作步骤：
1. 将 `pdf转excel` 目录完整上传至服务器（推荐路径 `/opt/pdf转excel`）；
2. 在服务器终端中进入该目录并执行：
   ```bash
   cd /opt/pdf转excel
   docker-compose up -d --build
   ```
3. 验证部署状态：
   ```bash
   docker ps
   ```
   显示 `ecg_pdf_parser_web` 容器状态为 `Up` 即表示服务启动成功！

---

## 4. 场景四：Linux 云服务器 (Ubuntu/CentOS) 原生部署 + 后台守护进程

适合在没有安装 Docker 的原生 Linux 服务器上进行长期后台运行。

### 1. 初始化运行环境
```bash
cd /opt/pdf转excel
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### 2. 启动方式 A：Nohup 简易后台运行
```bash
nohup python3 app.py > app.log 2>&1 &
```

### 3. 启动方式 B：Systemd 开机自启服务 (推荐生产级)
创建服务文件：
```bash
sudo nano /etc/systemd/system/ecg-app.service
```
写入配置：
```ini
[Unit]
Description=ECG PDF Parser Web Service
After=network.target

[Service]
User=root
WorkingDirectory=/opt/pdf转excel
ExecStart=/opt/pdf转excel/venv/bin/gunicorn --bind 0.0.0.0:5000 --workers 4 --threads 4 --timeout 600 app:app
Restart=always

[Install]
WantedBy=multi-user.target
```
启动并随系统开机自启：
```bash
sudo systemctl daemon-reload
sudo systemctl start ecg-app
sudo systemctl enable ecg-app
```

---

## 5. 场景五：Windows Server 服务器生产级部署 (NSSM 服务)

适合在医院或机构内部的 Windows Server 2016 / 2019 / 2022 服务器上部署。

### 操作步骤：
1. 将 `pdf转excel` 文件夹拷贝至服务器 `C:\pdf转excel`；
2. 打开 Windows 命令提示符 (CMD)：
   ```cmd
   cd C:\pdf转excel
   python -m venv venv
   call venv\Scripts\activate.bat
   pip install -r requirements.txt
   ```
3. 使用免费工具 [NSSM](https://nssm.cc/) 将其注册为 Windows 系统后台服务：
   ```cmd
   nssm install ECGParserService "C:\pdf转excel\venv\Scripts\python.exe" "C:\pdf转excel\app.py"
   nssm start ECGParserService
   ```

---

## 6. 场景六：医院局域网多台电脑共享访问

适合将服务运行在一台主电脑/服务器上，让医院科室内的所有医生电脑通过局域网 IP 免安装使用。

### 操作步骤：
1. **启动主服务**：在内网服务器或主电脑上按上述任意一种场景启动服务；
2. **获取内网 IP**：在主电脑上查询内网 IP（例如 `192.168.1.100`）；
3. **防火墙放行**：确保主电脑的 5000 端口在局域网防火墙中开放；
4. **共享网址**：科室里其他所有医生的电脑只需打开 Chrome / Edge 浏览器，输入 `http://192.168.1.100:5000` 即可直接共享访问！

---

## 7. 进阶补充：阿里云/腾讯云安全组放行 & Nginx 绑定域名/HTTPS

### 7.1 云服务器安全组放行 5000 端口 (⚠️ 公网部署必做)
如果是阿里云、腾讯云等公网服务器，外部用户访问前必须在控制台开放端口：
1. 登录云服务器控制台 $\rightarrow$ 进入 **“安全组”** / **“防火墙”**；
2. 添加**入站规则**：协议 `TCP`，端口 `5000`，授权对象 `0.0.0.0/0`；
3. 保存后即可通过 `http://您的公网IP:5000` 访问。

### 7.2 Nginx 绑定自定义域名 (隐藏 :5000 端口)
若希望用户输入 `http://ecg.yourdomain.com` 直接访问，可在 Nginx 配置文件中加入反向代理：
```nginx
server {
    listen 80;
    server_name ecg.yourdomain.com;

    client_max_body_size 500M;

    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```
配置完成后重启 Nginx（`sudo systemctl restart nginx`），外部用户即可直接通过域名免端口访问！
