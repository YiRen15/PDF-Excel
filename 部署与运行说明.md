# 动态心电图 PDF 转 Excel 自动化脚本部署与运行指南

该指南将指导您如何在您的 Mac 电脑上配置环境并运行 `convert.py` 自动化脚本。

---

## 1. 环境准备 (只需配置一次)

脚本运行需要使用 Python 3 以及两个第三方的 Python 处理库：`pdfplumber`（用于解析 PDF）和 `openpyxl`（用于生成/写入 Excel）。

### 第一步：打开终端 (Terminal)
在 Mac 上，您可以通过以下方式打开“终端”：
* 按下快捷键 `Command (⌘) + 空格键` 召唤 Spotlight 搜索，输入 **“终端”** 或 **“Terminal”**，回车打开。

### 第二步：进入脚本所在目录
在终端中输入以下命令并回车，进入您的桌面文件夹：
```bash
cd ~/Desktop/pdf转excel
```

### 第三步：创建虚拟环境 (推荐)
为了避免干扰系统自带的 Python 环境，建议在此文件夹下创建一个独立的“虚拟环境”：
```bash
python3 -m venv venv
```
*(如果此时系统提示您安装 Xcode Command Line Tools，请点击“安装”并耐心等待其下载安装完毕，然后再重新执行上述命令)*。

### 第四步：激活虚拟环境
在终端中执行：
```bash
source venv/bin/activate
```
激活成功后，您的命令行前端会出现 `(venv)` 字样。

### 第五步：安装依赖库
在激活状态下，执行以下命令安装解析所需的依赖包：
```bash
pip install --upgrade pip
pip install pdfplumber openpyxl
```
安装完成后，您的运行环境就全部部署好了！

---

## 2. 日常运行指南

当您后续放入了新的 PDF 报告，或者希望重新合并生成 Excel 时，请按照以下步骤运行：

1. **放入 PDF 文件**：
   将所有需要解析的 `.pdf` 报告文件放入桌面上的 `pdf转excel` 文件夹。

2. **自动读取空模板并输出**：
   - 脚本会自动读取当前目录下的 `随访心电记录仪检测记录_空模板.xlsx` 作为初始模板（请**不要**往此文件中手动写数据，保持其空白）。
   - 脚本运行后，会自动在同级目录下生成/刷新 `随访心电记录仪检测记录.xlsx` 文件。
   - 这样做可以确保您的**原始空模板永远不会被数据污染或覆盖**！

3. **运行脚本**：
   打开终端并依次输入以下命令：
   ```bash
   cd ~/Desktop/pdf转excel
   source venv/bin/activate
   python convert.py
   ```

4. **查看结果**：
   脚本运行完毕后会显示 `解析 PDF 文件...` 及 `写入完成`。此时您直接双击打开桌面目录下的 `随访心电记录仪检测记录.xlsx` 即可查看最新填充的数据行。

---

## 3. 常见问题排查

* **提示 `python3: command not found`**：
  说明您的 Mac 还没有安装 Python 环境。请访问 [Python 官方网站](https://www.python.org/downloads/) 下载并安装适用于 macOS 的最新安装包（如 Python 3.12+）。
* **想退出虚拟环境**：
  如果您在终端里完成了运行，可以输入 `deactivate` 退出虚拟环境，或者直接关闭终端窗口。
* **数据想要重新填，怎么操作？**
  脚本会读取该目录下的所有 `.pdf` 文件并生成一张全新的表格覆盖。如果您删除了某些 PDF，重新运行脚本后，Excel 中那些被删除的患者行也会自动消失。
