# Windows 电脑使用与运行指南

该网页应用系统是**完全跨平台支持**的。如果您或科室里的其他医生使用的是 Windows 电脑（如 Windows 10 / Windows 11），操作方式同样极其简单！

---

## 方式 1：Windows 单机双击即用 (推荐)

只要将桌面上整个 `pdf转excel` 文件夹复制到 Windows 电脑上，医生即可在 Windows 上双击使用。

### 首次使用前准备 (仅需 1 次)：
1. 确保 Windows 电脑上安装了 Python（如果没安装，双击 [Python 官方下载](https://www.python.org/downloads/) 勾选 `Add Python to PATH` 即可）。

### 医生日常使用 (双击图标)：
1. 打开 `pdf转excel` 文件夹；
2. 找到并**双击 `启动心电转换系统.bat`** 文件的图标；
3. 系统会自动配置环境，并**自动在浏览器（Edge / Chrome）中弹开系统网页** (`http://127.0.0.1:5000`)；
4. 拖入包含 PDF 报告的 ZIP 压缩包，解析完成后导出 Excel 即可！

---

## 方式 2：医院局域网服务器部署 (多人共享，无需每台 Windows 配环境)

如果您希望科室里的多位医生直接在各自的 Windows 电脑、iPad 甚至手机上打开网页就能用，无需在每台 Windows 上安装 Python：

1. **选定一台主电脑/服务器**（Mac、Windows 或 Linux 均可，只需在此一台电脑上运行系统）；
2. **获取主电脑的局域网 IP 地址**（例如：`192.168.1.100`）；
3. **其他医生电脑直接使用**：
   - 科室里其他所有 Windows 电脑的医生打开 Chrome / Edge 浏览器；
   - 在地址栏输入 `http://192.168.1.100:5000` 即可直接访问并使用网页系统上传、解析和下载！
